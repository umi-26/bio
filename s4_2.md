
아래에 제공하는 Mini PRD를 바탕으로
Claude Code에서 바로 구현할 수 있는
[4-2단계 : Mock MVP 개발용 PRD]로 변환해줘.

중요:
- 기존 Mini PRD에서 확정된 내용만 사용해줘.
- 필요할 것이라고 추측하여 새로운 기능, 화면, 데이터, 요구사항을 임의로 추가하지 말아줘.
- 회원가입, 로그인, 데이터베이스, 관리자 기능 등 기본 서비스 골격은 이번 MVP에서 제외해줘.
- 외부 AI API는 사용하지 말아줘.
- API Key 입력이나 유료 API 연동이 필요한 구조로 만들지 말아줘.
- 실제 OCR, LLM, 머신러닝 모델, 외부 데이터 분석 API가 없어도 전체 사용자 경험을 테스트할 수 있는 Mock MVP로 구현해줘.
- AI 분석이나 자동 생성이 필요한 기능은 실제 API 호출 대신, 이 서비스의 핵심 사용자와 사용 상황에 가장 적합한 더미 데이터, 더미 분석 결과, 예시 시나리오를 사용해 동작하도록 설계해줘.
- 더미 데이터는 단순 임의 예제가 아니라 아래 Mini PRD에서 정의된 사용자, 문제, 입력 데이터, 결과 구조를 반영해서 만들어줘.
- 실제 파일 내용을 분석하지 않더라도 사용자가 입력 → 처리 → 결과 흐름을 자연스럽게 체험할 수 있도록 구현해줘.
- 대표 더미 시나리오는 1개를 기본으로 하고, 핵심가설 검증에 필요하지 않다면 여러 시나리오를 추가하지 말아줘.
- 더미 분석 결과를 실제 AI 또는 실제 의료·전문 분석 결과처럼 오인하게 표현하지 말아줘.
- 의료·건강·법률·금융 등 전문 판단이 필요한 서비스라면 Mock 데이터는 예시 결과임을 명확히 구분하고, 실제 전문 판단을 생성하지 말아줘.
- 향후 실제 API를 연결할 수 있도록 분석 로직 영역은 별도 함수 또는 모듈로 분리해줘.
- 기술 스택 선택을 다시 묻지 말고 아래 개발환경을 기본값으로 적용해줘.
- 구현 방식에 여러 선택지가 있더라도 사용자에게 선택을 요청하지 말고, 아래 조건에 가장 적합한 단순한 방식을 선택해줘.
- 새로운 라이브러리는 꼭 필요한 경우에만 사용하고 기본 Next.js 기능과 단순한 구현을 우선해줘.
- 3시간 이내 실습에서 완성 가능한 구현 범위를 우선해줘.

[개발환경]

- Next.js
- TypeScript
- React
- Tailwind CSS
- GitHub 저장소 관리에 적합한 구조
- Vercel에 바로 배포 가능한 구조
- 프론트엔드와 필요한 간단한 서버 기능은 하나의 Next.js 프로젝트 안에서 구성
- 별도 Express 서버는 만들지 않음
- 별도 데이터베이스는 사용하지 않음
- 데이터가 필요한 경우 로컬 상수, JSON, mock 데이터 파일 등을 사용
- 이미지/PDF 업로드가 필요한 경우 실제 AI 분석 없이 업로드 UI와 더미 분석 흐름이 동작하도록 구현
- 업로드한 파일 자체를 서버나 외부 서비스에 전송하지 않아도 되는 구조를 우선 사용
- 환경변수나 외부 서비스 설정 없이 로컬 실행이 가능하도록 구성
- 별도 인증, 백엔드 인프라, 클라우드 스토리지를 추가하지 않음

아래 형식으로 정리해줘.

# Claude Code Mock MVP PRD

## 1. 프로젝트 개요
- 프로젝트명
- 서비스 목적
- 핵심 사용자
- 핵심 사업가설
- 이번 MVP에서 검증할 핵심가설

## 2. 개발 범위

### 이번 MVP에서 반드시 구현할 기능

### 구현하지 않을 기능

각 기능은 기존 Mini PRD 범위 안에서만 선정해줘.

## 3. User Flow

사용자 행동 기준으로

입력 → 처리 → 결과

순서로 작성해줘.

실제 AI 분석이 없는 부분은
어느 단계에서 mock 처리가 이루어지는지도 명확히 표시해줘.

## 4. 화면 구성

각 화면별로 아래 항목을 작성해줘.

- 화면명
- 화면 목적
- 주요 UI 요소
- 사용자 행동
- mock 처리 여부
- 다음 화면 또는 결과

Mini PRD에 없는 화면을 임의로 추가하지 말아줘.

## 5. 핵심 기능

각 기능별로 아래 항목을 작성해줘.

- 기능명
- 입력값
- 처리 방식
- 출력값
- 더미 데이터 또는 mock 처리 방식
- 향후 실제 기능/API 연결 시 교체할 영역

## 6. 데이터 구조

MVP에서 실제로 사용할 데이터 항목과 예시값을 작성해줘.

필요하면 TypeScript interface 형태도 함께 작성해줘.

다음 두 데이터를 구분해줘.

1. 사용자가 실제로 입력하는 데이터
2. Mock MVP 내부에서 사용하는 예시 데이터

Mini PRD에 없는 사용자 개인정보나 추가 입력항목을 임의로 만들지 말아줘.

## 7. 대표 Mock 시나리오

이 서비스의 핵심 사용자가 실제로 사용할 법한 대표 상황 1개를 선정해서 작성해줘.

- 사용자 상황
- 사용자 입력
- mock 처리
- 처리 결과
- 최종 출력 결과
- 이 시나리오를 통해 확인할 핵심가설

실제 데이터나 실제 전문 분석 결과처럼 표현하지 말고
테스트용 예시 시나리오로 작성해줘.

## 8. 프로젝트 구조

Next.js 기준으로 실제 구현에 필요한 폴더와 파일만 제안해줘.

예:

app/
components/
data/
lib/
types/

다음 원칙을 지켜줘.

- 필요하지 않은 폴더를 미리 만들지 않음
- 불필요한 추상화 금지
- 과도한 컴포넌트 분리 금지
- 이번 MVP에서 사용하지 않는 파일 생성 금지
- 향후 실제 분석 API로 교체할 mock 분석 로직만 별도 분리

## 9. 디자인 가이드

기존 Mini PRD에서 정해진 디자인 요구사항이 있으면 그대로 반영해줘.

정해진 디자인 요구사항이 없다면
새로운 브랜드 컨셉이나 복잡한 디자인 시스템을 만들지 말고
깔끔하고 읽기 쉬운 기본 웹 UI 수준으로 구성해줘.

우선순위:
1. 핵심 결과의 가독성
2. 사용자 흐름 이해
3. 모바일 대응
4. 시각적 완성도

## 10. Claude Code 구현 지시사항

Claude Code가 추가 질문 없이 바로 작업을 시작할 수 있도록
실행 순서 기준으로 구체적인 개발 지시를 작성해줘.

반드시 포함:

1. 현재 폴더와 Next.js 프로젝트 구조 확인
2. 필요한 페이지 생성
3. 필요한 공통 컴포넌트 생성
4. 대표 mock 데이터 생성
5. 사용자 입력 기능 구현
6. mock 분석 함수 또는 모듈 구현
7. 입력 → 분석 → 결과 사용자 흐름 연결
8. 결과 화면 구현
9. 기본 반응형 UI 적용
10. 오류 또는 빈 입력에 대한 최소한의 처리
11. 로컬 실행 및 빌드 오류 확인
12. Vercel에 배포 가능한 상태로 정리

중요:
- 오류 처리는 복잡한 예외처리 시스템이 아니라 MVP 실행에 필요한 최소 수준만 구현
- 테스트 코드, 관리자 기능, DB, 인증 등 Mini PRD에 없는 개발범위를 추가하지 말 것

## 11. MVP 완료 조건

다음 내용을 명확히 작성해줘.

- 사용자가 처음 화면에서 시작하여 최종 결과까지 도달할 수 있음
- 대표 Mock 시나리오가 정상 동작함
- Mini PRD에서 정의한 핵심 결과가 화면에 표시됨
- 핵심가설을 사용자에게 테스트할 수 있는 상태임
- 외부 API, API Key, DB 없이 로컬에서 실행 가능함
- build 오류 없이 Vercel 배포가 가능한 상태임

마지막에는 아래 제목으로 Claude Code에 그대로 전달할 수 있는 실행 명령문을 작성해줘.

# Claude Code 시작 명령

이 명령문에는 위 PRD의 핵심 요구사항과 개발환경을 포함해서,
추가 질문 없이 바로 파일 생성과 구현을 시작하도록 지시해줘.

중요:
- 계획만 설명하고 멈추지 말 것
- 기술 스택을 다시 묻지 말 것
- API 사용 여부를 다시 묻지 말 것
- 데이터베이스 사용 여부를 다시 묻지 말 것
- 구현 방법을 사용자에게 선택하게 하지 말 것
- 임의 기능을 추가하지 말 것
- Mini PRD에 없는 사용자 입력을 추가하지 말 것
- mock 데이터를 실제 분석 결과처럼 표현하지 말 것
- 위 PRD 범위 안에서 바로 파일 생성과 구현을 시작할 것
- 구현 후 로컬 실행 및 build 오류까지 확인할 것

[아래에 4-1단계에서 생성한 Mini PRD 전체를 붙여넣기]
    
